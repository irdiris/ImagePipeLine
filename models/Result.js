class Result {
    constructor(imageInfos, width, height) {
        this.imageInfos = imageInfos; // array of ImageInfo instances
        this.width = width;
        this.height = height;
    }
}

module.exports = Result;